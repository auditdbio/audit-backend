Implement module `vars` with sled-stored variable `last_modified` with type `i64`.

The module should be implemented in `vars` module of the `fts` crate.

The variable should be initialized with the value of `0`.

The module should implement the trait with the following methods:

1. Get the value of the variable
2. Set the value of the variable

The module should be tested using temporary sled database.

Run `cargo clippy` after each edit, espessialy edit of `Cargo.toml`, to prevent errors.
